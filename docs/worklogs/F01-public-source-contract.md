# F01 首个真实来源与合规采集契约

- 更新时间：2026-07-14 18:33（Asia/Shanghai）
- 状态：已完成
- 负责人/窗口：Codex F01 工程师（recovery/task-08-baseline）
- 依赖任务：C00（已完成）
- 声明文件范围：
  - `docs/SOURCE_CCGP.md`
  - `docs/worklogs/F01-public-source-contract.md`
  - `docs/WORK_PLAN.md`（仅在全部门禁通过后更新 F01 状态与唯一入口）
- 明确不修改：
  - `app/**`、`lib/**`、`backend/app/**`、`backend/tests/**`
  - `docs/worklogs/TASK-10-product-chain.md`
  - 数据库、fixture、账号、Cookie、Token、API Key

## 阶段记录

### 2026-07-14 — 阶段 0：认领与基线核对

#### 已完成

- 阅读 F01 指定的项目计划、上下文、统一数据契约、工作日志规范、TASK-02 日志、现有 CCGP 适配器与定向测试。
- 确认分支为 `recovery/task-08-baseline`，起始 HEAD 为 `c3764e0e7fcff1683c692abd7519a5f1baec3464`，起始工作树无未提交差异。
- 声明本任务唯一允许修改范围；后续来源实现、工作流、存储、前端、报告、调度、测试和 TASK-10 日志均不在范围内。

#### 改动文件

- `docs/worklogs/F01-public-source-contract.md`：创建独立日志并声明文件边界。

#### 验证结果

- 命令/检查：`git status --short --branch`、`git diff`、`git diff --check`、`git rev-parse --verify HEAD`
- 结果：通过。
- 证据：目标分支正确；HEAD 解析为指定提交；起始 diff 与 `git diff --check` 均无输出。

#### 阻塞

- 无。

#### 下一步

- 只使用中国政府采购网官方页面、`robots.txt`、平台声明/条款等一手材料核验来源身份、检索、详情与附件入口，并记录无法确认项。

### 2026-07-14 18:20 — 阶段 1：官方一手来源核验

#### 已完成

- 官方首页返回 HTTP 200，核对到权威发布媒体说明、财政部国库司主办单位和网站标识码 `bm14000002`。
- 官方检索入口返回 HTTP 200；一次带主题、页码和日期窗口的串行查询返回真实结果容器。未把搜索摘要用作证据。
- 两个真实公告详情返回 HTTP 200：一个覆盖标题、发布时间、采购人、地区、预算、项目编号、截止时间和正文；另一个覆盖附件入口并显示附件可能指向站外对象存储。
- 主站与检索站 `robots.txt` 均返回 HTTP 404；官方页面中未找到自动化访问条款或官方速率上限。已明确记录为“无法确认”，未把 404 解释为授权。
- 复核真实失败页面状态：主站品牌 404、检索站纯文本 404、网页阅读器路径的详情 403（同一 URL 后续直接 GET 为 200）、无参数检索的 HTTP 200 表单页。
- 全程只读、串行、至少 2 秒间隔；未登录、未处理验证码、未下载公告站外附件、未保存响应文件。

#### 改动文件

- `docs/SOURCE_CCGP.md`：记录官方证据、无法确认项和访问边界。

#### 验证结果

- 命令/检查：官方 URL 网页直读；.NET `HttpClient` 低频 GET，输出状态、最终 URL、类型、页面标记与有限样本。
- 结果：通过（一次工具参数失败和一次沙箱网络失败后，按权限流程复跑成功）。
- 证据：检索/三个详情/首页/联系页为 200；两个 robots 路径为预期 404；参数化检索存在结果容器，详情存在字段与附件入口。
- 失败记录：本机 `Invoke-WebRequest` 不支持 `-SkipHttpErrorCheck`，在参数绑定阶段未发请求；沙箱内 .NET 请求报传输错误，获准进行只读网络访问后复跑成功。

#### 阻塞

- 自动化条款、官方速率上限、站外附件主机授权和登录态附件规则无法从本次官方材料确认。
- 解除条件：官方发布相应规则或项目责任人取得书面确认；在此之前只允许按保守内部契约运行，不阻塞 F01 文档完成。

#### 下一步

- 完成字段、访问、合规、失败和现有实现映射契约，再执行静态与链接门禁。

### 2026-07-14 18:24 — 阶段 2：契约成稿与首轮门禁

#### 已完成

- 定义标题、项目编号、采购人、公告类型、发布时间、预算、截止时间、地区、正文、附件、来源 URL、采集时间和字段级证据定位。
- 定义 0.5 请求/秒、单主机并发 1、最少 2 秒间隔、可识别 User-Agent、超时、有限重试、分页、时间窗口、缓存和复合水位线。
- 定义 robots/条款未知时的保守边界、验证码/频控/安全页停止、禁止绕过、禁止凭据落盘、站外附件与异常附件停止条件。
- 定义 DNS、超时、403/429、安全页、4xx/5xx、结构变化、附件异常、真实空结果和来源故障的稳定错误码、重试、停止与水位线副作用。
- 逐项映射 `backend/app/sources/ccgp.py`，分为“已实现”“需后续实现”“无法确认”，并明确代码不是网站授权证据。

#### 改动文件

- `docs/SOURCE_CCGP.md`：完成 10 节来源与合规采集契约。
- `docs/worklogs/F01-public-source-contract.md`：追加研究、契约和验证记录。

#### 验证结果

- 命令/检查：必需章节/字段扫描、声明范围扫描、敏感值扫描、禁用成功标记扫描、Markdown 链接语法/主机检查、8 个官方链接低频状态复核、`git diff --check`。
- 结果：当前通过；最终双轴审查和总计划更新尚未执行。
- 证据：8 个 Markdown 目标均为 HTTPS CCGP 官方主机；6 个页面为 200，两个 robots 目标按记录为 404；敏感值与禁用标记扫描无命中；只修改两个已声明文件。
- 警告：`gitleaks` 和 `agent-reach` 可执行文件不可用，分别使用分项正则与官方网页/直接 HTTP 复核替代；Git 报告 LF 将在下次触碰时按工作区规则转为 CRLF。
- 失败记录：首次 intent-to-add 因 `.git/index.lock` 沙箱权限失败，获准写本地索引后成功；两次复合 PCRE 扫描因本机解析/参数规则失败，改用 .NET 分项正则后通过；首次链接提取把代码反引号纳入 URL，改用 Markdown 目标解析后通过。

#### 阻塞

- 无 F01 文档阻塞；总计划保持未更新，等待全部门禁。

#### 下一步

- 执行 Standards/Spec 双轴独立审查；修复全部发现后完成最终扫描，才允许更新 `docs/WORK_PLAN.md`。

### 2026-07-14 18:30 — 阶段 3：Standards/Spec 双轴审查

#### 已完成

- Standards 首审发现 1 个判断性命名问题：附件失败使用了未声明的 `attachment_partial`。已改为契约内声明的 `status=partial` 加稳定附件错误码；复审 0 项，PASS。
- Spec 首审发现 3 个现有实现映射过度乐观的问题：自动重定向在最终主机检查前可能已发出站外请求；`urlopen(timeout)` 不是异步端到端总时限；标题/发布时间只读 meta 而未与可见值交叉核对。三项均改为“需后续实现”并写明缺口；复审 0 项，PASS。
- 两轴均确认未发现范围蔓延；总计划尚未更新符合门禁顺序。

#### 改动文件

- `docs/SOURCE_CCGP.md`：统一结构化附件状态命名，并纠正三项现有实现映射。
- `docs/worklogs/F01-public-source-contract.md`：记录两轴发现、修复和复审结果。

#### 验证结果

- 命令/检查：以 `c3764e0e7fcff1683c692abd7519a5f1baec3464` 为固定点，对工作树差异运行独立 Standards 与 Spec 审查；修复后由原审查者复核。
- 结果：通过。
- 证据：Standards 最终 0 项；Spec 最终 0 项；范围蔓延 0 项；复审 `git diff --check` 通过，仅有工作区 LF→CRLF 提示。

#### 阻塞

- 无。

#### 下一步

- 在最终全量门禁通过后更新 `docs/WORK_PLAN.md`，随后复核最终三文件差异并创建单一本地提交。

### 2026-07-14 18:33 — 阶段 4：最终门禁与计划移交

#### 已完成

- 最终必需条款扫描确认来源、日期、官方入口、全部字段、访问参数、合规边界、失败码和三类实现映射均存在。
- 最终声明范围、禁用标记、敏感值、Markdown 链接、官方主机和 `git diff --check` 门禁通过。
- 8 个官方链接已按至少 2 秒间隔逐一复核：6 个内容页为 HTTP 200，两个 robots 路径按证据为 HTTP 404。
- 在全部前置门禁通过后，才将 `docs/WORK_PLAN.md` 的 F01 更新为“已完成”，并把唯一关键路径入口切换为依赖满足且文件不重叠的 F02。
- 未修改生产来源、工作流、存储、前端、报告、调度、测试、fixture、数据库或 TASK-10 日志；未创建临时响应文件。

#### 改动文件

- `docs/SOURCE_CCGP.md`：新增首个权威来源及字段、访问、合规、失败和实现映射契约。
- `docs/worklogs/F01-public-source-contract.md`：新增 F01 独立认领、研究、门禁、双轴审查和移交记录。
- `docs/WORK_PLAN.md`：门禁后更新 F01 状态与唯一下一任务 F02。

#### 验证结果

- 命令/检查：必需条款扫描、声明范围检查、禁用标记扫描、.NET 分项敏感值扫描、Markdown 链接解析/主机检查、8 URL 低频状态复核、`git diff --check`、Standards/Spec 双轴复审。
- 结果：通过。
- 证据：43 个必需词项齐全；禁用标记与敏感值无命中；8 个链接主机/状态符合记录；Standards 0 项、Spec 0 项、范围蔓延 0 项。
- 补充测试：`python -B -m unittest tests.test_ccgp_source -v` 未执行。首次命令因 `python` 不在 PATH 失败；仓库 `backend/.venv/Scripts/python.exe` 又指向不存在的另一用户运行时。未安装或改写环境，按“环境不可用，跳过”记录；F01 未修改实现或测试。
- 可选工具：`gitleaks` 与 `agent-reach` 可执行文件不可用，分别以分项敏感值扫描和官方网页/直接 HTTP 复核替代。
- 警告：Git 持续提示新增 Markdown 的 LF 将在下次工作区触碰时按配置转为 CRLF；`git diff --check` 无错误。

#### 阻塞

- 无。自动化条款、官方速率、站外附件授权和登录态附件规则继续作为契约内“无法确认”项，等待官方材料，不影响本次 F01 文档验收。

#### 下一步

- 创建包含上述三个文件的单一本地提交；不 push。随后下一任务只能依据最新 `docs/WORK_PLAN.md` 进入 F02。

## 安全检查

- [x] 未将账号写入仓库或日志。
- [x] 未将 Cookie 写入仓库或日志。
- [x] 未将 Token 写入仓库或日志。
- [x] 未将 API Key 写入仓库或日志。
- [x] 仅记录了无敏感值的环境变量名或配置状态。

## 完成验收

- [x] `docs/WORK_PLAN.md` 中该任务的验收条件全部满足。
- [x] 所有改动都在声明文件范围内。
- [x] 改动文件和验证结果已记录。
- [x] 已完成安全检查。
- [x] 已通知协调窗口更新总计划状态。
